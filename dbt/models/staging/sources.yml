version: '1.0.0'

sources:
  - name: raw
    description: "Raw retail rocket data csv files"
    database: analytics_events
    schema: raw
    tables:
      - name: events
        description: "Events raw table"
      
      - name: category_tree
        description: "Category tree raw table"
      
      - name: item_properties
        description: "Item properties raw table"